请把后续多条即将被裁切出短期上下文的聊天消息压缩成聊天回想。
要求：
1. 只根据后续消息总结，不要编造没有出现的信息。
2. summary 是完整摘要，保留话题脉络、人物立场、已达成结论、待办和重要细节，适合之后恢复上下文。
3. recall_cues 输出 3-5 个用于之后语义匹配的查询式段落。请额外思考“什么情景下可能会需要这些信息”“这些信息和什么内容、人物、任务、关键词关联”，每条写成自然语言短句，包含可匹配的场景、意图或关联内容。
4. 只输出一个 JSON 对象，不要使用 Markdown 代码块或额外解释，格式必须为 {{"summary":"...","recall_cues":["..."]}}。
5. 后续聊天记录会按原始多消息格式逐条提供，请保留消息边界带来的上下文关系。
6. 这是内部记忆，人物以 person_id（及其 platform、user_id）为主标识；summary 和 recall_cues 提到具体人物时保留其 ID 与昵称/群名片的对应关系。同名不同 ID 是不同人物，同 ID 改名仍是同一人。只依据消息中的自身标记识别机器人，不根据昵称或 assistant 角色猜测，不为缺少身份信息的人编造 ID。之后对用户回复时应优先使用昵称。

时间范围：{time_range}
参与人物：{participants_text}
